# Changelog

本文档记录 LLM Project 的主要版本变化。

---

## Unreleased（开发中）

### Added

#### 角色扩展元信息（补齐与酒馆卡的差距 · 批1）

- 新增 `CharacterMeta` 模型与 `characters.meta_json` 列（数据库 v2 → v3 安全迁移）
- 统一承载标签 / 作者 / 作者备注 / 角色版本 / 来源格式 /
  历史后注入指令(post_history_instructions) / 示例对话(mes_example)
- 角色详情面板展示「角色信息」（标签 / 作者 / 版本 / 来源 / 作者备注）
- `.llmcard` 导出 / 导入、完整备份均携带 `meta_json`
- 角色卡转换工具现在会写入 `meta_json`（标签 / 作者 / 来源等不再仅靠条目承载）

> 说明：元信息默认不注入 Prompt，仅用于展示与保留。标签筛选、
> 历史后注入位、示例对话注入将在后续批次开放。

#### 角色信息编辑与展示（批2）

- 角色编辑页新增「角色信息」编辑区：标签（逗号分隔）、作者、版本、作者备注
- 编辑页保存时写入 `meta_json`（保留转换器带来的 source_format / post_history 等字段）
- 角色库卡片展开时显示标签（最多 4 个）
- 这些信息手动新建的角色也能填写使用，不依赖转换器

#### 角色卡转换工具输出格式与位置

- 转换结果优先输出为图片角色卡 `.llmchar.png`（与导出格式一致），
  在文件管理器中可直接看到角色立绘缩略图，更直观；无封面图时回退 `.llmcard`
- Android 转换结果保存到系统「Download/LLM Project/Converted Cards/<时间戳>」
  （与备份 / 导出一致，方便在文件管理器中查看），不再留在应用内部目录
- 桌面端仍保存到系统 Downloads/LLM Project/Converted Cards
- 产物可被角色库「导入角色卡」直接识别（含导入预览）

#### 占位符统一渲染（{{char}} / {{user}}）

- 发送给模型前，对整个 system prompt（角色 system_prompt 字段 / 角色设定条目 /
  世界书 / 周期摘要等）统一替换 {{char}} → 角色名、{{user}} → 用户名
- 此前仅「角色扮演规则 / 连续性提醒 / 历史后指令」会替换，导致条目与世界书里的
  占位符被模型当作字面量读取；现已与酒馆行为一致
- 占位符为引擎原生支持，手写卡与导入卡均可直接使用，无需转换

#### 历史后注入 / post_history_instructions（批3）

- 新增「历史后指令」：把指令放到对话历史最末尾（贴近模型回复位置），
  比开头 system 更不易在长对话中被淡忘
- 角色编辑页「角色信息」新增「历史后指令」编辑框，支持 {{char}} / {{user}}
- Prompt 策略页新增「注入历史后指令」开关（默认开，已纳入备份）
- 所有发送路径（发送 / 重新生成 / 继续写 / Prompt 预览）一致生效
- 兼容酒馆 post_history_instructions：导入的酒馆卡此项现在真正生效

#### 角色库标签筛选（批2 收尾）

- 角色库顶部新增标签筛选栏（仅当存在标签时显示，可横向滚动）
- 点击标签只显示带该标签的角色，「全部」恢复显示
- 筛选结果为空时显示友好提示

#### 第三方角色卡转换工具（005-A，规则转换）

- 新增纯 Dart 转换核心 `lib/tools/character_converter/`（PC 与移动端共用）
- 支持识别 PNG 内嵌角色卡（tEXt / zTXt / iTXt 的 `chara` / `ccv3`）
- 支持 SillyTavern / Character Card V2 与 TavernAI / V1 JSON
- 字段映射到 LLM Project 角色结构（名称 / 描述 / 性格 / 场景 / 示例对话 /
  开场白 / 备用开场白 / 系统提示 / 后置指令 / 来源信息 / 作者备注）
- 内嵌 `character_book` 转换为 LLM Project 世界书并自动绑定角色
- 世界书高级触发规则（优先级 / 位置 / 深度 / 概率等）降级并在报告中提示
- 完整保留原始第三方 JSON（禁用条目），便于后续重新转换
- 输出标准 `.llmcard`（可被「导入角色卡」直接识别）+ 转换报告（json/txt）
- 桌面端支持拖入文件 / 文件夹批量转换，移动端支持多选文件
- 设置页新增「角色卡转换工具」入口
- 新增单元测试 `test/character_converter_test.dart`

> 不执行酒馆脚本 / 正则 / 扩展插件，只做解析、映射与保留。
> AI 智能归类与精修对照为后续增强（005-B/C）。

### Changed

#### 世界书库页面级令牌化 & 词云封面

- 删除按钮 / 删除确认文字改用 `danger`、导入预览成功勾选改用 `success`、
  编辑浮层遮罩改用 `scrim`，接入主题令牌（Day / Night 自动适配）
- 世界书封面中间的「首字标识」升级为**词云**：统计所有条目标题 + 主关键词的
  出现频率，每个词用**圆形气泡**裹住、气泡越大语义越重（最多 7 个词）；
  按**正六边形蜂窝**排列——语义最重的词放最中间，其余按圈向外展开，
  气泡内部字号随气泡大小同步变化，且气泡带**随机浮动**动画（各自独立
  的相位/幅度/频率，缓慢错落浮动；用整数倍频保证循环重置时位置连续、
  不闪烁）；用 `Expanded` 占满中间空间、
  `FittedBox` 等比缩放，不会溢出到下方信息栏；无词汇时回退为首字标识
- 程序生成封面上的白色文字/装饰保持不变（叠在有色彩封面上，令牌化会破坏可读性）

#### 角色库 & 角色编辑页体验微调

- 选中角色的信息浮层改为**三块贴边玻璃抽屉**：名字贴左上（从左往右展开，
  宽最多卡片宽 1/2，超长省略）、标签贴右上（从上往下展开占高 2/3，垂直
  竖排文字、最多两列，超出「+x」）、介绍贴左下（从左往右展开、最多两行，
  超长省略）；玻璃片带紫色调（亮主题深紫 / 暗主题浅紫），贴边一侧直角
- 选中浮层动画：开启动画 + **关闭动画**（反向收回）+ **换选角色重新播放**
  （用自持 AnimationController 的 `_SelectedOverlay`，始终挂载在卡片上，
  通过 `active` 驱动正/反向）
- 角色库的播放（进入聊天）悬浮按钮改为**选中一张角色卡片时才显示**，
  未选中时隐藏，避免常驻按钮与"先选再进"的交互冲突
- 点击播放按钮后：按钮进入**史莱姆加载动画**（旋转一圈 → 压扁 → 弹起 →
  停歇几秒再循环），同时后台预热聊天数据，UIEngine 就绪后选中卡片**放大
  进入聊天页**（复用主页 `stagedRole` 成熟转场，加载不再卡顿/白屏）
- 角色库进入聊天的转场改用**简化直放式**（`HomeTransitions.cardToChat`）：
  不用主页的 accent 渐变+同心环封面，直接展示**角色卡真实封面图**，卡片从
  角色库中的自身位置放大到全屏，不再有"中间态居中悬停卡片+底部加载胶囊"
- 点击播放按钮后**先不放大**：右下角按钮史莱姆动画循环，后台用
  `precacheImage` 把封面图解码进图片缓存，并留足最小加载时长，
  **封面加载完毕后才**触发放大跳转，避免放大过程中封面
  还是空白/未加载，也让「先右下角加载 → 再放大」的节奏清晰
- 放大转场进一步调短（约 500ms 一次连续放大到满屏，放大到几乎满屏的瞬间
  同步淡化、聊天页随之显现，干脆利落）
- 播放键的转圈现在持续到 **UIEngine 真正就绪**：点击后 offstage 预加载一个
  ChatPage 触发真实初始化，就绪前播放键保持史莱姆转圈，就绪后播放键消失、
  立即放大跳转（不再 push 时就盖掉按钮、转场里干等）
- 修复从主页进入聊天页后**退出时缺少返回动画**：`stagedRole` 反向（pop）时
  现在会把 stage 从满屏拉回主页角色入口，卡片从全屏收缩回原位 + 页面淡出，
  与进场对称，不再只剩页面淡出、没有卡片收缩
- 角色编辑浮层面板底色由 `scaffoldBackgroundColor(canvas)` 改为 `surfaceElevated`：
  夜间 canvas 近乎纯黑，与背后黑色遮罩叠在一起边界感弱，提亮一档让面板更突出

#### 聊天页核心区 UI 令牌化（对齐主页风格 · 第一批）

- 新增聊天区语义色板（`_bubbleUser` / `_bubbleAssistant` / `_onBubblePrimary` /
  `_onBubbleMuted` / `_avatarFallback` 等），替代散落的 `_isNight ? X : Y` 三元
- 消息气泡：用户气泡用 `accentSoft`（Day 淡紫 / Night 向 accent 提亮的蓝紫）、
  助手气泡用 `surface`/`surfaceElevated` + 描边，Day/Night 下收发对比均清晰可辨
- 状态栏进度条 track 改用 `outline`、value 改用 `accent`，深色模式下不再发黑难辨
- 用户气泡编辑态颜色、头像占位、功能图标、Tokens 标签、角色名胶囊、
  输入栏毛玻璃底色、气泡/HTML/对白高亮正文颜色均接入主题令牌
- 输入栏与角色名胶囊改用 `surfaceGlass`，Day / Night 自动适配
- 骨架屏消息气泡同步更新，避免加载态与真实内容颜色跳变
- 仅改核心聊天区（消息气泡 / 列表 / 输入栏），未动布局、交互与功能分支

---

## Demo 1.2.5

> 当前版本为 Demo 阶段测试版本，重点完善角色卡、世界书、背景、备份与资产导入导出能力。

### Added

#### 角色卡与系统卡

- 新增人物卡 / 系统卡类型区分
- 新增角色卡条目化编辑
- 新增系统卡主角设定
- 新增主角设定嵌套字段平铺编辑
- 新增角色头像与 2:3 卡片封面支持
- 新增角色切换轮盘
- 新增轮盘角色详情页
- 新增角色名底部胶囊入口
- 新增角色卡导入 / 导出能力
- 新增完整角色卡文件包 `.llmcard`
- 新增图片角色卡 `.llmchar.png`
- 支持角色卡导出时可选包含绑定世界书

#### 开场白

- 新增多开场白管理
- 新增开场白切换显示
- 新增无历史记录时自动插入首条开场白
- 修复新角色进入聊天页后开场白不显示的问题

#### 用户设定与主角设定

- 新增当前卡用户覆盖设定
- 新增全局用户设定作为默认昵称与头像
- 新增系统卡主角设定作为用户默认设定
- 新增“恢复默认设定”与“保存为角色卡默认”逻辑

#### 世界书

- 新增世界书管理
- 新增世界书关键词触发
- 新增常驻条目
- 新增递归触发
- 新增世界书导入 / 导出 `.llmworld.json`
- 新增世界书导入前预览确认
- 新增程序生成式世界书封面

#### 背景系统

- 新增背景图库
- 新增图片背景
- 新增纯色 / 渐变背景
- 新增角色独立背景
- 新增图片背景卡导入 / 导出
- 背景卡导出保留原图比例

#### 备份与恢复

- 新增完整备份 `.llmbak`
- 新增自定义导出内容
- 支持导出角色卡、世界书、背景、图片资源、用户设定、API 配置、聊天记录等
- 支持导入完整备份
- 支持备份导出到 `Download / LLM Project / Backups`
- 新增 API Key 和聊天记录敏感导出提醒

#### 资产系统

- 新增内部资产识别标识
- 导入时不依赖文件扩展名
- 新增导入前预览与完整性检查
- 新增角色卡、世界书、背景卡资产导入导出
- 新增导出到系统 Download 分类目录

#### Android 适配

- 新增网络权限配置
- 新增 `UCropActivity` 配置
- 新增状态栏 / 刘海屏适配
- 新增 Android MediaStore 下载目录保存逻辑

---

### Changed

- 优化角色卡编辑页布局
- 优化角色设定字段编辑方式
- 优化系统卡主角设定显示方式
- 优化聊天页角色名位置
- 优化角色轮盘交互入口
- 优化资源库排序交互
- 新建按钮合并为“新建 / 导入”入口
- 导出按钮合并为导出方式选择
- 角色库进入对话按钮移动到右下角
- 世界书库封面由灰色图标改为程序生成式封面
- 背景卡导出由固定比例改为原图比例
- 备份导出由自定义路径改为固定保存到 Download 分类目录

---

### Fixed

- 修复 Release APK 无法联网的问题
- 修复隐藏系统状态栏后顶部黑框问题
- 修复 image_cropper 选择图片后闪退问题
- 修复角色卡开场白首次进入不显示问题
- 修复角色卡编辑页多处像素溢出问题
- 修复角色库按创建时间排序不稳定问题
- 修复背景库 / 世界书库时间排序问题
- 修复 Android 公共目录写入权限导致备份导出失败的问题
- 修复导入 `.llmbak` 文件时系统文件选择器无法点击的问题

---

### Known Issues

- 当前仍为 Demo 阶段，软件名称、图标和部分 UI 仍可能调整
- 图片角色卡如果被聊天软件压缩，内部数据可能丢失
- 世界书高级触发机制尚未完善
- 状态栏功能目前仍处于基础占位阶段
- 输入功能区部分按钮仍为后续规划
- 角色心声 / 内心独白功能尚未实现
- 部分资产格式未来可能升级，但会尽量保持向后兼容

---

## Demo 1.3.x Preview

计划重点：

- 正式确定软件名称与图标
- 品牌命名统一
- 资产格式稳定化
- 状态栏功能实装
- Prompt 分频注入策略
- 角色设定版本管理
- 世界书高级触发机制
- 输入功能区完善
- 角色心声功能探索
